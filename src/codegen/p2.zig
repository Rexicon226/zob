//! Backend for emitting P2 (Paralax Propeller 2) assembly

const Oir = @import("../Oir.zig");
const Recursive = Oir.extraction.Recursive;

pub fn generate(recv: *const Recursive) !void {
    _ = recv;
}
